db_name: art-reports
db_user: art-reports
db_password: art-reports
db_host: 127.0.0.1
db_port: 5432

apache_modules:
  - ssl
  - wsgi
  - expires
  - headers
  - rewrite

hostname: "{{inventory_hostname}}"
apache_user: www-data
git_repo: "https://git.linaro.org/people/milosz.wasilewski/art-db-backend.git"
git_tag: celery
django_app_root: "{{install_base}}/{{hostname}}/project"
django_settings: crayonbox.settings
django_local_settings: templates/art_reports_local_settings.py
django_local_wsgi: templates/art_reports_index.wsgi
install_base: /srv

supervisord_conf: "{{django_app_root}}/supervisord.conf"
supervisord_path: "{{install_base}}/{{hostname}}/.virtualenv/bin/supervisord"
supervisor_config_dir: "{{django_app_root}}/supervisor.conf.d"

ssl_key: /etc/ssl/private/ssl-cert-snakeoil.key
ssl_cert: /etc/ssl/certs/ssl-cert-snakeoil.pem
ssl_ca: /etc/ssl/certs/ca-certificates.crt
